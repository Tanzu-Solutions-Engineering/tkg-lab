apiVersion: apps/v1
kind: Deployment
metadata:
  name: fortune-app-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fortune-app-prod
  template:
    metadata:
      name: fortune-app-prod
      labels:
        app: fortune-app-prod
      annotations:
        prometheus.io/path: '/actuator/prometheus'
        prometheus.io/port: '8080'
        prometheus.io/scrape: 'true'  
    spec:
      containers:
      - image: azwickey/fortune-demo:latest
        name: fortune-app-prod
        ports:
        - containerPort: 8080
          protocol: TCP
        env:
        - name: SPRING_REDIS_HOST
          value: "fortune-redis-master.default.svc.cluster.local"
